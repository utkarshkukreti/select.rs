language: rust
rust:
  - stable
  - beta
  - nightly
script:
  - cargo build --verbose
  - "[ $TRAVIS_RUST_VERSION = nightly ] && cargo test --verbose || exit 0"
  - "[ $TRAVIS_RUST_VERSION = nightly ] && cargo bench --verbose || exit 0"
